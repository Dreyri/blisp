package org.blisp;

import org.blisp.nodes.Expression;
import org.objectweb.asm.ClassWriter;
import org.objectweb.asm.MethodVisitor;

import java.util.Arrays;
import java.util.List;

public interface ISymbolTable {
    /*
    String getScopeName();
    // this shouldn't increment the register count
    int nextRegister();
    void emitLoad(String name, List<ClassWriter> classWriters, ClassWriter currentClass, MethodVisitor currentMethod);
    //void emitInvoke(String name, Expression[] args, List<ClassWriter> classWriters, ClassWriter currentClass, MethodVisitor mv);
     */
}
