package org.blisp;

import org.blisp.nodes.Class;
import org.blisp.nodes.Define;

public class TypeChecker {
    public TypeChecker()
    {

    }

    /*
    public void typeCheck(Class startNode) throws Exception
    {
        ClassScope scope = new ClassScope();

        // do a pass and add all symbols first
        for (Define def : startNode.definitions)
        {
            scope.pushSymbol(def.name, def.expr);
        }
    }
     */
}
